var arango;

try {
    arango = require('arango')
} catch (e) {
    arango = require('../..')
}

// define port to make browser tests work in ci
exports.db = arango.Connection({_server:{port:8529}});
