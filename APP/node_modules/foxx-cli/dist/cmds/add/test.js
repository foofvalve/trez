"use strict";

function _asyncToGenerator(fn) { return function () { var gen = fn.apply(this, arguments); return new Promise(function (resolve, reject) { function step(key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { return Promise.resolve(value).then(function (value) { step("next", value); }, function (err) { step("throw", err); }); } } return step("next"); }); }; }

const { white } = require("chalk");
const { common } = require("../../util/cli");
const { fatal } = require("../../util/log");
const { generateTest } = require("../../generator");
const fs = require("../../util/fs");
const path = require("path");

const command = exports.command = "test <name>";
exports.detestion = "Add a test";
const describe = 'Creates a test file under "tests/<name>.js" and adds the pattern "test/**/*.js" to the manifest.json if its property "test" is undefined.';

const args = [["name", "Name of the test to add."]];

exports.builder = yargs => common(yargs, { command, describe, args }).example("$0 add test example", 'Adds a test "example" to the local service');

exports.handler = (() => {
  var _ref = _asyncToGenerator(function* (argv) {
    const manifestPath = path.resolve(process.cwd(), "manifest.json");
    if (!(yield fs.exists(manifestPath))) {
      fatal("Current directory does not contain a manifest file.");
    }
    const tests = path.resolve(process.cwd(), "test");
    if (!(yield fs.exists(tests))) {
      yield fs.mkdir(tests);
    }
    const test = path.resolve(tests, `${argv.name}.js`);
    if (yield fs.exists(test)) {
      fatal(`Test "${white(test)}" already exists.`);
    }
    yield fs.writeFile(test, (yield generateTest()));
    const manifest = JSON.parse((yield fs.readFile(manifestPath)));
    if (!manifest.tests) {
      manifest.tests = "test/**/*.js";
      yield fs.writeFile(manifestPath, JSON.stringify(manifest, null, 2));
    }
  });

  function handler(_x) {
    return _ref.apply(this, arguments);
  }

  return handler;
})();